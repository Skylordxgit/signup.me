# Copy this file to .env.local for local development, or configure these
# as environment variables in Hostinger for production.

# MySQL connection string. Leave unset locally to use the built-in JSON file
# store (data/db.json). Required in production.
# Format: mysql://db_user:db_password@db_host:3306/db_name
DATABASE_URL=

# Long random value used to sign admin session cookies. Generate with:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=

# Admin login credentials. If unset, defaults to admin@example.com / admin123
# (local development only — always set these in production).
ADMIN_EMAIL=
ADMIN_PASSWORD_HASH=

# Public base URL used to build absolute links (og:image, canonical URLs, etc).
NEXT_PUBLIC_APP_URL=

# Set to "true" in production so session cookies require HTTPS.
COOKIE_SECURE=false

# Where uploaded images are written. Must be writable by the Node process and
# should live outside the deploy directory so uploads survive a redeploy.
# Defaults to data/uploads. Files are served from /uploads/<category>/<name>;
# only the path is stored in the database, never the image itself.
UPLOAD_DIR=

# Maximum accepted upload size in bytes (default 5MB). Images are compressed
# and resized in the browser before upload, so this applies to the final file.
MAX_UPLOAD_BYTES=
